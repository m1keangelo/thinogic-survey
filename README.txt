v4.2.1 — proven v4.1.4 client (all 6 steps work) + server hardened with Claude v4.2.0 good ideas. CONNECTION FIXES: Bearer prefix restored (both proxy branches), region api.moonshot.ai (your key is global, not .cn). Added: /api/health, AUTH_USERS multi-login, session expiry sweep, image upload + retrieval (/api/image), safe CSRF (rejects mismatched Origin). Footer reads v4.2.1 / v4.2.1.
